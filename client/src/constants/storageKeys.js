/**
 * @fileoverview Local storage key constants shared across auth store and API client.
 */

/** @type {'token'} JWT access token storage key. */
export const AUTH_TOKEN_KEY = 'token';

/** @type {'refreshToken'} JWT refresh token storage key. */
export const REFRESH_TOKEN_KEY = 'refreshToken';
